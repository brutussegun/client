import type { NextPage } from "next";

import { MainNavigation } from "../components/Navigation";

const Home: NextPage = () => {
  return (
    <div>
      <MainNavigation />
    </div>
  );
};

export default Home;
